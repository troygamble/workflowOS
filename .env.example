# Clerk — required for sign-in (AI routes require an authenticated session)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Optional server-side OpenAI fallback (users can also BYOK in Studio settings)
# OPENAI_API_KEY=sk-...
