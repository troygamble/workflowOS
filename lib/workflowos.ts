/**
 * lib/workflowos.ts
 *
 * Single source of truth for WorkflowOS (PSF Workflow Studio) as an
 * open-source project. WorkflowOS is free and MIT-licensed: run it hosted at
 * /studio, or self-host from the public repo. The institute does not provide
 * paid support — it is an AS-IS reference implementation and an Integrator
 * value-add.
 *
 * TROY: publish the public repo, then set WORKFLOWOS_GITHUB_URL below to the
 * real URL. The default is a placeholder; everything else reads from here.
 */
export const WORKFLOWOS_GITHUB_URL = "https://github.com/troygamble/workflowOS";
export const WORKFLOWOS_LICENSE = "MIT";
export const WORKFLOWOS_NAME = "WorkflowOS";
